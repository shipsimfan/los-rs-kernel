pub fn handler(
    _: usize,
    _: usize,
    _: usize,
    _: usize,
    _: usize,
    _: usize,
    _: interrupts::Registers,
    _: u64,
    _: u64,
    _: u64,
) -> base::error::Result<isize> {
    Ok(0)
}
